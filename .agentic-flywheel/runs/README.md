# Runs

Create one folder per substantial task where the agent and user iterated on the result.

For public repositories, keep these run notes local only and do not commit task-specific contents.

Suggested naming:

- `YYYY-MM-DD-short-slug`

Each run folder can contain:

- `run.md`
- recommendation files created during that task
